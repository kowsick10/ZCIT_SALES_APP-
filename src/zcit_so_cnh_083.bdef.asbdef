projection;
strict ( 2 );
use draft;

define behavior for ZCIT_SO_CNH_083 alias SalesOrderHdr
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Resume;
  use action Prepare;
  use action Activate;
  use action Discard;

  use association _salesitem { create; with draft; }
}

define behavior for ZCIT_SO_CNI_083 alias SalesOrderItm
{
  use update;
  use delete;

  use association _salesHeader { with draft; }
}